@{
    ViewData["Title"] = "My RimColonist";
}

<div class="text-center">
    <h1 class="display-4">Find out your rimworld colonist stats by taking the quiz below!</h1>
    <button type="button" class="btn btn-primary btn-lg" onclick="@("window.location.href='" + @Url.Action("StartQuiz", "Home") + "'");">Start</button>
</div>
